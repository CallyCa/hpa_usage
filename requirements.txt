# Python dependencies for the HPA usage artifact.
#
# These exact direct-dependency versions are shared by every Python environment
# recorded in the compatibility matrix in the README.
# Install with:
#
#     python -m pip install -r requirements.txt

# Notebook execution
jupyter==1.1.1
ipykernel==6.29.5

# Data handling and analysis (Notebooks 1, 2 and 3)
pandas==2.2.3
numpy==2.0.2
scipy==1.13.1

# Plotting (Notebooks 1, 2 and 3)
matplotlib==3.9.4
seaborn==0.13.2

# Manifest parsing and GitHub access (Notebook 1 and the bootstrap cells)
pyyaml==6.0.2
requests==2.32.3
urllib3==2.2.3
python-dateutil==2.9.0.post0
python-dotenv==1.0.1

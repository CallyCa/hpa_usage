# Python dependencies for the HPA usage artifact.
#
# Lower bounds are the versions considered safe for these notebooks. The exact
# versions used in the last successful run are listed under "Tested versions"
# in the README. Install with:
#
#     python -m pip install -r requirements.txt

# Notebook execution
jupyter>=1.0
ipykernel>=6.0

# Data handling and analysis (Notebooks 1, 2 and 3)
pandas>=1.5
numpy>=1.23
scipy>=1.9

# Plotting (Notebooks 1, 2 and 3)
matplotlib>=3.6
seaborn>=0.12

# Manifest parsing and GitHub access (Notebook 1 and the bootstrap cells)
pyyaml>=6.0
requests>=2.28
urllib3>=1.26
python-dateutil>=2.8
python-dotenv>=1.0
